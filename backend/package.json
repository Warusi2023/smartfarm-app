{
  "name": "smartfarm-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=18.19.0 <19" },
  "scripts": {
    "prepare": "git rev-parse --is-inside-work-tree >/dev/null 2>&1 && husky install || echo 'husky skipped'",
    "build": "echo \"no TS build\"",
    "start": "node server.cjs",
    "dev": "node server.cjs",
    "doctor": "node -e \"console.log(process.version);require('fs').access('server.cjs',0,err=>console.log('server.cjs', err?'MISSING':'OK'));\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}