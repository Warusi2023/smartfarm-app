package com.smartfarm.shared

import android.content.Context

// Temporary no-op initializer for Android.
fun initializeAndroidApp(context: Context) {
    // No shared DB or Koin setup for first release.
}
