{
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "npm install",
    "buildCommand": "echo 'Static site ready'"
  },
  "deploy": {
    "startCommand": "npx http-server ./public -p $PORT -a 0.0.0.0 -c-1",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60
  }
}
