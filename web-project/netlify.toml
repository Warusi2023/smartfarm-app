[build]
  command = "npm install --include=dev && npm run build"
  publish = "dist"

# Redirect rules live in public/_redirects (copied to dist/_redirects on build).
# Do not duplicate [[redirects]] here — merged rules can let SPA /* win over /api/*.
