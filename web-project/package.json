{
  "name": "smartfarm-frontend",
  "version": "1.0.2",
  "description": "SmartFarm Frontend - Static Dashboard (Railway Ready)",
  "scripts": {
    "start": "node server.js",
    "dev": "npx http-server ./public -p 8080 -a 0.0.0.0 -c-1",
    "build": "echo 'Static site build completed - files ready in public/'",
    "deploy": "echo 'Deployment ready - No Husky conflicts'",
    "railway": "node server.js",
    "netlify": "npm run build"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}