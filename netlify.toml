[build]
  # Tell Netlify to run everything inside the web-project folder
  base = "web-project"

  # Install deps and build the Vite app
  command = "npm install && npm run build"

  # This is relative to the base ("web-project"), so it points to web-project/dist
  publish = "dist"
