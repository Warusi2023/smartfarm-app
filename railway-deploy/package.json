{
  "name": "smartfarm-railway-deploy",
  "version": "2.0.0",
  "description": "SmartFarm Railway Deployment - Static Site Only",
  "scripts": {
    "start": "npx http-server ./public -p $PORT -a 0.0.0.0 -c-1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
